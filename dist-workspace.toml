[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.30.2"
# CI backends to support
ci = "github"
# A prefix git tags must include for dist to care about them
tag-namespace = "santa-v"
# The installers to generate for each app
installers = ["shell", "powershell", "npm", "homebrew"]
# Enable cargo-auditable to embed dependency info in binaries for vulnerability scanning
cargo-auditable = true
# Enable GitHub artifact attestations for supply chain security
github-attestations = true
# Generate CycloneDX SBOM (bom.xml) for supply chain transparency
cargo-cyclonedx = true
# Generate OmniBOR artifact IDs for provenance tracking
omnibor = true
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Build only the required packages, and individually
precise-builds = true
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
